name: Testing Secret Variables

on:
  workflow_dispatch
env:
      ACTIONS_STEP_DEBUG: true

jobs:
  test_contect_variables:
    runs-on: ubuntu-latest
    steps:
     - name: "Checking Secret Variable"
       run: 
         echo "Secret Value : ${{ secrets.MY_KEY }}"
